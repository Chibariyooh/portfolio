<h1>Users#show</h1>
<p>Find me in app/views/users/show.html.erb</p>

<% if user_signed_in? && @user == current_user %>
    <h1>マイページ</h1>
    <%= link_to "プロフィール編集", edit_user_registration_path(current_user) %>
    <%= link_to "削除", user_path, method: :delete, data: {confirm: "削除しますか?"} %>
<% else %>
    <h1>ユーザーページ</h1>
<% end %>

<h1><%= @user.username %></h1>

<% if user_signed_in? && @user == current_user %>
    <% link_to "プロフィール編集", edit_user_registration_path(current_user) %>
<% else %>
<% end %>